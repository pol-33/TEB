# mapper-speed

This directory is reserved for the future speed-focused mapper variant.

The intended design is a minimizer plus hash-based mapper optimized for wall-clock runtime. Only build skeletons are included in this phase.
